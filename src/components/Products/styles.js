import { makeStyles } from '@material-ui/core/styles';

export default makeStyles((theme) => ({
  content: {
    marginTop: '2.5rem',
    marginBottom: '2.5rem',
    minHeight: '40vh',
  },
  grid: {
    alignItems: 'flex-start',
  },
}));
